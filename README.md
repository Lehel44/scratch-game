# scratch-game

## Usage:

- build: ```mvn -T 4 clean install```

- There will be two jars, use the one that's named "*jar-with-dependencies.jar".

- ```java -jar target/scratch-game-1.0-SNAPSHOT-jar-with-dependencies.jar --config src/test/resources/config.json --betting-amount 100.0```
